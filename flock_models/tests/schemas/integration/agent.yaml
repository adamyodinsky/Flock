---

apiVersion: flock/v1
kind: Agent
metadata:
  name: my-agent
  description: A Q&A agent for internal projects
  labels:
    app: my_app
namespace: default
spec:
  dependencies:
    - kind: LLM
      name: my-openai-llm
      namespace: default
  tools:
    - kind: VectorStoreQATool
      name: langchain-docs
          